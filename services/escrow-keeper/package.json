{
  "name": "@natt-pundit/escrow-keeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "F96N P1 — auto-settle devnet escrow pools (permissionless settle ix, fee payer only)",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "bs58": "^4.0.1"
  }
}
