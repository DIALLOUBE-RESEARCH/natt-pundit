node_modules/
.next/
dist/
.env
.env.*
!.env.example
*.log
apps/web/public/sw.js
apps/web/public/workbox-*.js
apps/web/public/fallback-*.js

# F86N v2 — proprietary edge formula (VPS/private only, never commit)
packages/natt-edge-engine/

# Nano Banana flag PNGs (~1MB each) — prod uses CDN circle-flags; regenerate locally only
apps/web/public/ui/flags/*.png
